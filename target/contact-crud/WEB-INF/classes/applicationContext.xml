<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
    <property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
    <property name="url"
              value="#{ 'jdbc:mysql://' + systemEnvironment['MYSQLHOST']
                      + ':' + systemEnvironment['MYSQLPORT']
                      + '/' + systemEnvironment['MYSQLDATABASE']
                      + '?serverTimezone=UTC'}" />
    <property name="username" value="#{ systemEnvironment['MYSQLUSER'] }" />
    <property name="password" value="#{ systemEnvironment['MYSQLPASSWORD'] }" />
</bean>
